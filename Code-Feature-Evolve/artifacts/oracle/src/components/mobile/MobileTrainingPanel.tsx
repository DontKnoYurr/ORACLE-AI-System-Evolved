/**\n * Mobile-Optimized Training Control Panel\n * \n * Provides comprehensive training management on mobile devices with\n * touch-friendly controls, real-time metrics, and manual triggers.\n */\n\nimport React, { useState, useEffect } from 'react';\nimport { Button } from '@/components/ui/button';\nimport { Card } from '@/components/ui/card';\nimport { Slider } from '@/components/ui/slider';\nimport { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';\nimport { Badge } from '@/components/ui/badge';\nimport { AlertCircle, Play, Pause, RotateCcw, Zap, TrendingUp } from 'lucide-react';\n\ninterface TrainingMetrics {\n  epoch: number;\n  loss: number;\n  accuracy: number;\n  learningRate: number;\n  gradientNorm: number;\n}\n\ninterface MobileTrainingPanelProps {\n  isTraining: boolean;\n  currentMetrics: TrainingMetrics | null;\n  onStartTraining: (config: any) => Promise<void>;\n  onStopTraining: () => Promise<void>;\n  onResetTraining: () => Promise<void>;\n  onGenerateSynthetic: (count: number) => Promise<void>;\n}\n\nconst MobileTrainingPanel: React.FC<MobileTrainingPanelProps> = ({\n  isTraining,\n  currentMetrics,\n  onStartTraining,\n  onStopTraining,\n  onResetTraining,\n  onGenerateSynthetic,\n}) => {\n  const [epochs, setEpochs] = useState(50);\n  const [learningRate, setLearningRate] = useState(0.001);\n  const [optimizer, setOptimizer] = useState('adam');\n  const [syntheticCount, setSyntheticCount] = useState(500);\n  const [isLoading, setIsLoading] = useState(false);\n\n  const handleStartTraining = async () => {\n    setIsLoading(true);\n    try {\n      await onStartTraining({\n        epochs,\n        learningRate,\n        optimizer,\n      });\n    } finally {\n      setIsLoading(false);\n    }\n  };\n\n  const handleGenerateSynthetic = async () => {\n    setIsLoading(true);\n    try {\n      await onGenerateSynthetic(syntheticCount);\n    } finally {\n      setIsLoading(false);\n    }\n  };\n\n  return (\n    <div className=\"w-full max-w-md mx-auto p-4 space-y-4\">\n      {/* Status Header */}\n      <Card className=\"p-4 bg-gradient-to-r from-blue-900 to-blue-800 border-blue-700\">\n        <div className=\"flex items-center justify-between\">\n          <div>\n            <p className=\"text-sm text-blue-200\">Training Status</p>\n            <p className=\"text-2xl font-bold text-white\">\n              {isTraining ? 'ACTIVE' : 'IDLE'}\n            </p>\n          </div>\n          <div className=\"text-right\">\n            <div className=\"flex items-center gap-2 justify-end\">\n              {isTraining && (\n                <div className=\"w-3 h-3 bg-green-400 rounded-full animate-pulse\" />\n              )}\n              <Badge variant={isTraining ? 'default' : 'secondary'}>\n                {isTraining ? 'Running' : 'Ready'}\n              </Badge>\n            </div>\n          </div>\n        </div>\n      </Card>\n\n      {/* Current Metrics */}\n      {currentMetrics && (\n        <Card className=\"p-4 space-y-3\">\n          <h3 className=\"font-semibold text-lg\">Current Metrics</h3>\n          \n          <div className=\"grid grid-cols-2 gap-3\">\n            <div className=\"bg-slate-50 p-3 rounded\">\n              <p className=\"text-xs text-slate-600\">Epoch</p>\n              <p className=\"text-xl font-bold text-slate-900\">{currentMetrics.epoch}</p>\n            </div>\n            <div className=\"bg-slate-50 p-3 rounded\">\n              <p className=\"text-xs text-slate-600\">Loss</p>\n              <p className=\"text-xl font-bold text-slate-900\">{currentMetrics.loss.toFixed(4)}</p>\n            </div>\n            <div className=\"bg-slate-50 p-3 rounded\">\n              <p className=\"text-xs text-slate-600\">Accuracy</p>\n              <p className=\"text-xl font-bold text-green-600\">{(currentMetrics.accuracy * 100).toFixed(2)}%</p>\n            </div>\n            <div className=\"bg-slate-50 p-3 rounded\">\n              <p className=\"text-xs text-slate-600\">Learning Rate</p>\n              <p className=\"text-xl font-bold text-slate-900\">{currentMetrics.learningRate.toExponential(2)}</p>\n            </div>\n          </div>\n\n          {/* Progress Bar */}\n          <div className=\"space-y-2\">\n            <div className=\"flex justify-between items-center\">\n              <span className=\"text-sm font-medium\">Gradient Norm</span>\n              <span className=\"text-sm text-slate-600\">{currentMetrics.gradientNorm.toFixed(4)}</span>\n            </div>\n            <div className=\"w-full bg-slate-200 rounded-full h-2\">\n              <div\n                className=\"bg-blue-500 h-2 rounded-full transition-all duration-300\"\n                style={{\n                  width: `${Math.min((currentMetrics.gradientNorm / 10) * 100, 100)}%`,\n                }}\n              />\n            </div>\n          </div>\n        </Card>\n      )}\n\n      {/* Training Configuration */}\n      <Tabs defaultValue=\"config\" className=\"w-full\">\n        <TabsList className=\"grid w-full grid-cols-2\">\n          <TabsTrigger value=\"config\">Config</TabsTrigger>\n          <TabsTrigger value=\"synthetic\">Synthetic</TabsTrigger>\n        </TabsList>\n\n        <TabsContent value=\"config\" className=\"space-y-4\">\n          <Card className=\"p-4 space-y-4\">\n            {/* Epochs Slider */}\n            <div className=\"space-y-2\">\n              <label className=\"text-sm font-medium flex items-center gap-2\">\n                <Zap className=\"w-4 h-4\" />\n                Epochs: {epochs}\n              </label>\n              <Slider\n                value={[epochs]}\n                onValueChange={(value) => setEpochs(value[0])}\n                min={10}\n                max={200}\n                step={10}\n                disabled={isTraining}\n                className=\"w-full\"\n              />\n            </div>\n\n            {/* Learning Rate Slider */}\n            <div className=\"space-y-2\">\n              <label className=\"text-sm font-medium flex items-center gap-2\">\n                <TrendingUp className=\"w-4 h-4\" />\n                Learning Rate: {learningRate.toExponential(2)}\n              </label>\n              <Slider\n                value={[Math.log10(learningRate)]}\n                onValueChange={(value) => setLearningRate(Math.pow(10, value[0]))}\n                min={-5}\n                max={-2}\n                step={0.1}\n                disabled={isTraining}\n                className=\"w-full\"\n              />\n            </div>\n\n            {/* Optimizer Selection */}\n            <div className=\"space-y-2\">\n              <label className=\"text-sm font-medium\">Optimizer</label>\n              <div className=\"grid grid-cols-3 gap-2\">\n                {['adam', 'rmsprop', 'sgd'].map((opt) => (\n                  <Button\n                    key={opt}\n                    variant={optimizer === opt ? 'default' : 'outline'}\n                    size=\"sm\"\n                    onClick={() => setOptimizer(opt)}\n                    disabled={isTraining}\n                    className=\"text-xs\"\n                  >\n                    {opt.toUpperCase()}\n                  </Button>\n                ))}\n              </div>\n            </div>\n\n            {/* Control Buttons */}\n            <div className=\"flex gap-2 pt-4\">\n              <Button\n                onClick={handleStartTraining}\n                disabled={isTraining || isLoading}\n                className=\"flex-1 gap-2\"\n                size=\"lg\"\n              >\n                <Play className=\"w-4 h-4\" />\n                Start\n              </Button>\n              <Button\n                onClick={onStopTraining}\n                disabled={!isTraining || isLoading}\n                variant=\"outline\"\n                className=\"flex-1 gap-2\"\n                size=\"lg\"\n              >\n                <Pause className=\"w-4 h-4\" />\n                Stop\n              </Button>\n              <Button\n                onClick={onResetTraining}\n                disabled={isTraining || isLoading}\n                variant=\"ghost\"\n                size=\"lg\"\n              >\n                <RotateCcw className=\"w-4 h-4\" />\n              </Button>\n            </div>\n          </Card>\n        </TabsContent>\n\n        <TabsContent value=\"synthetic\" className=\"space-y-4\">\n          <Card className=\"p-4 space-y-4\">\n            <div className=\"flex items-start gap-2 p-3 bg-amber-50 border border-amber-200 rounded\">\n              <AlertCircle className=\"w-4 h-4 text-amber-600 mt-0.5 flex-shrink-0\" />\n              <p className=\"text-sm text-amber-800\">\n                Generate synthetic training data to augment sparse real-world data.\n              </p>\n            </div>\n\n            {/* Synthetic Count Slider */}\n            <div className=\"space-y-2\">\n              <label className=\"text-sm font-medium\">Data Points: {syntheticCount}</label>\n              <Slider\n                value={[syntheticCount]}\n                onValueChange={(value) => setSyntheticCount(value[0])}\n                min={100}\n                max={2000}\n                step={100}\n                disabled={isLoading}\n                className=\"w-full\"\n              />\n            </div>\n\n            {/* Distribution Info */}\n            <div className=\"grid grid-cols-2 gap-2 text-sm\">\n              <div className=\"bg-slate-50 p-2 rounded\">\n                <p className=\"text-xs text-slate-600\">Seismic Pulses</p>\n                <p className=\"font-semibold\">{Math.floor(syntheticCount * 0.25)}</p>\n              </div>\n              <div className=\"bg-slate-50 p-2 rounded\">\n                <p className=\"text-xs text-slate-600\">Quantum Collapses</p>\n                <p className=\"font-semibold\">{Math.floor(syntheticCount * 0.20)}</p>\n              </div>\n              <div className=\"bg-slate-50 p-2 rounded\">\n                <p className=\"text-xs text-slate-600\">Temporal Obs</p>\n                <p className=\"font-semibold\">{Math.floor(syntheticCount * 0.20)}</p>\n              </div>\n              <div className=\"bg-slate-50 p-2 rounded\">\n                <p className=\"text-xs text-slate-600\">Anomalies</p>\n                <p className=\"font-semibold\">{Math.floor(syntheticCount * 0.15)}</p>\n              </div>\n            </div>\n\n            {/* Generate Button */}\n            <Button\n              onClick={handleGenerateSynthetic}\n              disabled={isLoading}\n              className=\"w-full gap-2\"\n              size=\"lg\"\n            >\n              <Zap className=\"w-4 h-4\" />\n              Generate Synthetic Data\n            </Button>\n          </Card>\n        </TabsContent>\n      </Tabs>\n    </div>\n  );\n};\n\nexport default MobileTrainingPanel;\n
